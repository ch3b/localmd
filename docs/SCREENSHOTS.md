# Screenshots

Suggested screenshots to include:

1. Start screen (light mode)
2. Reader screen (light mode)
3. Start screen (dark mode)
4. Reader screen (dark mode)

Use `scripts/capture-screenshots.sh` and `scripts/capture-screenshots-dark.sh`.
